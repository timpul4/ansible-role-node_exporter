---
# - name: Install Prometheus Node Exporter
#   hosts: test-servers
#   become: true
#   vars_prompt:
#     - name: version_node_exporter
#       prompt: Enter node exporter version
#   tasks:
#     - name: Prepair block
#       delegate_to: 127.0.0.1
#       run_once: true
#       block: 
#         - name: Get list-releases
#           ansible.builtin.uri:
#             url: https://api.github.com/repos/prometheus/node_exporter/releases
#           register: git_answer
  
#         - name: Debug git_answer
#           ansible.builtin.set_fact:
#             list_version: "{{ git_answer.json | map(attribute='tag_name') }}"
          
#         - name: Assert
#           ansible.builtin.assert: 
#             that: version_node_exporter in list_version
#             fail_msg:  "incorrect version,choose from list: {{ list_version|join(', ') }}"

#     - name: OS-ARHITECT
#       ansible.builtin.set_fact:
#         os_architecter: "{{ version_node_exporter  | regex_replace('^v', '') }}.linux-{{ 'amd64' if ansible_facts['architecture'] == 'x86_64' else ansible_facts['architecture'] }}"
      

#     - name: Create link for download
#       ansible.builtin.set_fact:
#         release_node_exporter: "https://github.com/prometheus/node_exporter/releases/download/{{ version_node_exporter }}/node_exporter-{{ os_architecter }}.tar.gz"

#     - name: Download from link
#       ansible.builtin.unarchive:
#         src: "{{ release_node_exporter }}"
#         dest: /usr/local/bin
#         remote_src: yes
#         include:
#           - "node_exporter-{{ os_architecter }}/node_exporter"
#         extra_opts:
#           - "--strip-component=1"




